{
  "name": "google-drive-vault-sync",
  "version": "0.1.14",
  "description": "Two-way Google Drive sync for vaults.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "release:tag": "git push && git push --tags",
    "release:create": "gh release create v$npm_package_version main.js manifest.json styles.css --title \"v$npm_package_version\" --generate-notes",
    "release": "npm run build && git tag v$npm_package_version && npm run release:tag && npm run release:create"
  },
  "keywords": [
    "obsidian",
    "google-drive",
    "sync"
  ],
  "author": "Haniewicz",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/qrcode": "^1.5.6",
    "esbuild": "^0.21.5",
    "obsidian": "^1.5.12",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
